## Notes
- Add augmentation to regularize (crop to MVSEC resolution, flipping, random crop)
- Play with hyperparameters
- Decrease the network size (fully CNN)
- Fully connected network?
- up until 0.4 difference in reproduction is fine (so it is okay)