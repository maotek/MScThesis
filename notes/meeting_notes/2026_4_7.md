## Remarks
- Train a single 5ch -> 3ch conv (instead of representation learner).
    - To see whether the nonlinear UNet/FC adds useful information.

- Train the UNet/FC with 1 channel output (instead of 3) IN PROGRESS
    - Check whether color has something to do, or grey scale is on par.

- Permute reconstructed RGB, drop each channel. 
    - Check which channel has most effect.

- Calculate mean/std of reconstructed RGB channels and compare against raw DSEC RGB.
    - See whether Mean/Std has an effect.

- Compare intermediate features of DAv2 (DINOv2 encoder)
    - Compare the intermediate features of reconstructed RGB and original RGB
    - Check whether UNet/FC intermediate features are close to RGB than tencode.

- Checking effect of imagenet normalization on: rgb, unet, unet_1c

- Baselines
    - 5ch voxelgrid -> UNet/FC -> 3ch -> frozen DAv2
    - 5ch voxelgrid -> UNet/FC -> 3ch -> partial frozen (only encoder/decoder) DAv2
    - 5ch voxelgrid -> UNet/FC -> 3ch -> finetune DAv2
    - 5ch voxelgrid -> single 1x1 conv -> 3ch -> frozen DAv2

- Use DepthPro as second VFM?
    - Check whether generalizes across VFMs (rather than datasets)

## Notes
- Read the recent paper of RMSE 4.*
- Try computational efficiency of larger model to see the impact of that (compared to vitsmall)